fn main() {

    // Literais
    let x = 5;
    let y = 199_456_989_9;  // pode usar _ para ficar mais lengivel quando o numero é muito grande
    let h = 0xff; // hexadecimal
    let o = 0o77; // Octal
    let b = 0b1111_0000; // binario
    let by = b'A';  // byte

    // Float
    let x = 42.1;// Float na maioria das vezes usa f64, dificilmente usa f32

    // bolean
    let v = true;
    let f = false;

    // Char pode ser usado até 4 bytes, pode ser simbolos como emoji
    let letra = 'a'; // precisa usar aspas simples
    let emoji = '😊';
}
